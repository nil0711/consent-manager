<section>
  <h1>Something went wrong</h1>
  <p><%= message %></p>
  <p><a href="<%= (user && user.role === 'researcher') ? '/researcher' : '/participant' %>">
    ← Back to <%= (user && user.role === 'researcher') ? 'your studies' : 'all studies' %>
  </a></p>
</section>
